### Stellar-distribution-script

A simple script to get and distill data for a 3D render of the galaxy using astroquery with Gaia.

## Installation

Set environment
```
python3 -m venv venv
source venv/bin/activate
```

Install necessary packages
```
pip install astropy[recommended] astroquery[all] pandas --upgrade 
```
